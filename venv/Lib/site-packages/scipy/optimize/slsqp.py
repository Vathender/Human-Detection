"""
This module implements the Sequential Least Squares Programming optimization
algorithm (SLSQP), originally developed by Dieter Kraft.
See http://www.netlib.org/toms/733

Functions
---------
.. autosummary::
   :toctree: generated/

    approx_jacobian
    fmin_slsqp

"""

__all__ = ['approx_jacobian', 'fmin_slsqp']

import warnings
import numpy as np
from scipy.optimize._slsqp import slsqp
from numpy import (zeros, array, linalg, append, asfarray, concatenate, finfo,
                   sqrt, vstack, exp, inf, isfinite, atleast_1d)
from .optimize import (OptimizeResult, _check_unknown_options,
                       _prepare_scalar_function, _clip_x_for_func,
                       _check_clip_x)
from ._numdiff import approx_derivative
from ._constraints import old_bound_to_new, _arr_to_scalar


__docformat__ = "restructuredtext en"

_epsilon = sqrt(finfo(float).eps)


def approx_jacobian(x, func, epsilon, *args):
    """
    Approximate the Jacobian matrix of a callable function.

    Parameters
    ----------
    x : array_like
        The state vector at which to compute the Jacobian matrix.
    func : callable f(x,*args)
        The vector-valued function.
    epsilon : float
        The perturbation used to determine the partial derivatives.
    args : sequence
        Additional arguments passed to func.

    Returns
    -------
    An array of dimensions ``(lenf, lenx)`` where ``lenf`` is the length
    of the outputs of `func`, and ``lenx`` is the number of elements in
    `x`.

    Notes
    -----
    The approximation is done using forward differences.

    """
    # approx_derivative returns (m, n) == (lenf, lenx)
    jac = approx_derivative(func, x, method='2-point', abs_step=epsilon,
                            args=args)
    # if func returns a scalar jac.shape will be (lenx,). Make sure
    # it's at least a 2D array.
    return np.atleast_2d(jac)


def fmin_slsqp(func, x0, eqcons=(), f_eqcons=None, ieqcons=(), f_ieqcons=None,
               bounds=(), fprime=None, fprime_eqcons=None,
               fprime_ieqcons=None, args=(), iter=100, acc=1.0E-6,
               iprint=1, disp=None, full_output=0, epsilon=_epsilon,
               callback=None):
    """
    Minimize a function using Sequential Least Squares Programming

    Python interface function for the SLSQP Optimization subroutine
    originally implemented by Dieter Kraft.

    Parameters
    ----------
    func : callable f(x,*args)
        Objective function.  Must return a scalar.
    x0 : 1-D ndarray of float
        Initial guess for the independent variable(s).
    eqcons : list, optional
        A list of functions of length n such that
        eqcons[j](x,*args) == 0.0 in a successfully optimized
        problem.
    f_eqcons : callable f(x,*args), optional
        Returns a 1-D array in which each element must equal 0.0 in a
        successfully optimized problem. If f_eqcons is specified,
        eqcons is ignored.
    ieqcons : list, optional
        A list of functions of length n such that
        ieqcons[j](x,*args) >= 0.0 in a successfully optimized
        problem.
    f_ieqcons : callable f(x,*args), optional
        Returns a 1-D ndarray in which each element must be greater or
        equal to 0.0 in a successfully optimized problem. If
        f_ieqcons is specified, ieqcons is ignored.
    bounds : list, optional
        A list of tuples specifying the lower and upper bound
        for each independent variable [(xl0, xu0),(xl1, xu1),...]
        Infinite values will be interpreted as large floating values.
    fprime : callable `f(x,*args)`, optional
        A function that evaluates the partial derivatives of func.
    fprime_eqcons : callable `f(x,*args)`, optional
        A function of the form `f(x, *args)` that returns the m by n
        array of equality constraint normals. If not provided,
        the normals will be approximated. The array returned by
        fprime_eqcons should be sized as ( len(eqcons), len(x0) ).
    fprime_ieqcons : callable `f(x,*args)`, optional
        A function of the form `f(x, *args)` that returns the m by n
        array of inequality constraint normals. If not provided,
        the normals will be approximated. The array returned by
        fprime_ieqcons should be sized as ( len(ieqcons), len(x0) ).
    args : sequence, optional
        Additional arguments passed to func and fprime.
    iter : int, optional
        The maximum number of iterations.
    acc : float, optional
        Requested accuracy.
    iprint : int, optional
        The verbosity of fmin_slsqp :

        * iprint <= 0 : Silent operation
        * iprint == 1 : Print summary upon completion (default)
        * iprint >= 2 : Print status of each iterate and summary
    disp : int, optional
        Overrides the iprint interface (preferred).
    full_output : bool, optional
        If False, return only the minimizer of func (default).
        Otherwise, output final objective function and summary
        information.
    epsilon : float, optional
        The step size for finite-difference derivative estimates.
    callback : callable, optional
        Called after each iteration, as ``callback(x)``, where ``x`` is the
        current parameter vector.

    Returns
    -------
    out : ndarray of float
        The final minimizer of func.
    fx : ndarray of float, if full_output is true
        The final value of the objective function.
    its : int, if full_output is true
        The number of iterations.
    imode : int, if full_output is true
        The exit mode from the optimizer (see below).
    smode : string, if full_output is true
        Message describing the exit mode from the optimizer.

    See also
    --------
    minimize: Interface to minimization algorithms for multivariate
        functions. See the 'SLSQP' `method` in particular.

    Notes
    -----
    Exit modes are defined as follows ::

        -1 : Gradient evaluation required (g & a)
         0 : Optimization terminated successfully
         1 : Function evaluation required (f & c)
         2 : More equality constraints than independent variables
         3 : More than 3*n iterations in LSQ subproblem
         4 : Inequality constraints incompatible
         5 : Singular matrix E in LSQ subproblem
         6 : Singular matrix C in LSQ subproblem
         7 : Rank-deficient equality constraint subproblem HFTI
         8 : Positive directional derivative for linesearch
         9 : Iteration limit reached

    Examples
    --------
    Examples are given :ref:`in the tutorial <tutorial-sqlsp>`.

    """
    if disp is not None:
        iprint = disp

    opts = {'maxiter': iter,
            'ftol': acc,
            'iprint': iprint,
            'disp': iprint != 0,
            'eps': epsilon,
            'callback': callback}

    # Build the constraints as a tuple of dictionaries
    cons = ()
    # 1. constraints of the 1st kind (eqcons, ieqcons); no Jacobian; take
    #    the same extra arguments as the objective function.
    cons += tuple({'type': 'eq', 'fun': c, 'args': args} for c in eqcons)
    cons += tuple({'type': 'ineq', 'fun': c, 'args': args} for c in ieqcons)
    # 2. constraints of the 2nd kind (f_eqcons, f_ieqcons) and their Jacobian
    #    (fprime_eqcons, fprime_ieqcons); also take the same extra arguments
    #    as the objective function.
    if f_eqcons:
        cons += ({'type': 'eq', 'fun': f_eqcons, 'jac': fprime_eqcons,
                  'args': args}, )
    if f_ieqcons:
        cons += ({'type': 'ineq', 'fun': f_ieqcons, 'jac': fprime_ieqcons,
                  'args': args}, )

    res = _minimize_slsqp(func, x0, args, jac=fprime, bounds=bounds,
                          constraints=cons, **opts)
    if full_output:
        return res['x'], res['fun'], res['nit'], res['status'], res['message']
    else:
        return res['x']


def _minimize_slsqp(func, x0, args=(), jac=None, bounds=None,
                    constraints=(),
                    maxiter=100, ftol=1.0E-6, iprint=1, disp=False,
                    eps=_epsilon, callback=None, finite_diff_rel_step=None,
                    **unknown_options):
    """
    Minimize a scalar function of one or more variables using Sequential
    Least Squares Programming (SLSQP).

    Options
    -------
    ftol : float
        Precision goal for the value of f in the stopping criterion.
    eps : float
        Step size used for numerical approximation of the Jacobian.
    disp : bool
        Set to True to print convergence messages. If False,
        `verbosity` is ignored and set to 0.
    maxiter : int
        Maximum number of iterations.
    finite_diff_rel_step : None or array_like, optional
        If `jac in ['2-point', '3-point', 'cs']` the relative step size to
        use for numerical approximation of `jac`. The absolute step
        size is computed as ``h = rel_step * sign(x0) * max(1, abs(x0))``,
        possibly adjusted to fit into the bounds. For ``method='3-point'``
        the sign of `h` is ignored. If None (default) then step is selected
        automatically.
    """
    _check_unknown_options(unknown_options)
    iter = maxiter - 1
    acc = ftol
    epsilon = eps

    if not disp:
        iprint = 0

    # Transform x0 into an array.
    x = asfarray(x0).flatten()

    # SLSQP is sent 'old-style' bounds, 'new-style' bounds are required by
    # ScalarFunction
    if bounds is None or len(bounds) == 0:
        new_bounds = (-np.inf, np.inf)
    else:
        new_bounds = old_bound_to_new(bounds)

    # clip the initial guess to bounds, otherwise ScalarFunction doesn't work
    x = np.clip(x, new_bounds[0], new_bounds[1])

    # Constraints are triaged per type into a dictionary of tuples
    if isinstance(constraints, dict):
        constraints = (constraints, )

    cons = {'eq': (), 'ineq': ()}
    for ic, con in enumerate(constraints):
        # check type
        try:
            ctype = con['type'].lower()
        except KeyError as e:
            raise KeyError('Constraint %d has no type defined.' % ic) from e
        except TypeError as e:
            raise TypeError('Constraints must be defined using a '
                            'dictionary.') from e
        except AttributeError as e:
            raise TypeError("Constraint's type must be a string.") from e
        else:
            if ctype not in ['eq', 'ineq']:
                raise ValueError("Unknown constraint type '%s'." % con['type'])

        # check function
        if 'fun' not in con:
            raise ValueError('Constraint %d has no function defined.' % ic)

        # check Jacobian
        cjac = con.get('jac')
        if cjac is None:
            # approximate Jacobian function. The factory function is needed
            # to keep a reference to `fun`, see gh-4240.
            def cjac_factory(fun):
                def cjac(x, *args):
                    x = _check_clip_x(x, new_bounds)

                    if jac in ['2-point', '3-point', 'cs']:
                        return approx_derivative(fun, x, method=jac, args=args,
                                                 rel_step=finite_diff_rel_step,
                                                 bounds=new_bounds)
                    else:
                        return approx_derivative(fun, x, method='2-point',
                                                 abs_step=epsilon, args=args,
                                                 bounds=new_bounds)

                return cjac
            cjac = cjac_factory(con['fun'])

        # update constraints' dictionary
        cons[ctype] += ({'fun': con['fun'],
                         'jac': cjac,
                         'args': con.get('args', ())}, )

    exit_modes = {-1: "Gradient evaluation required (g & a)",
                   0: "Optimization terminated successfully",
                   1: "Function evaluation required (f & c)",
                   2: "More equality constraints than independent variables",
                   3: "More than 3*n iterations in LSQ subproblem",
                   4: "Inequality constraints incompatible",
                   5: "Singular matrix E in LSQ subproblem",
                   6: "Singular matrix C in LSQ subproblem",
                   7: "Rank-deficient equality constraint subproblem HFTI",
                   8: "Positive directional derivative for linesearch",
                   9: "Iteration limit reached"}

    # Set the parameters that SLSQP will need
    # meq, mieq: number of equality and inequality constraints
    meq = sum(map(len, [atleast_1d(c['fun'](x, *c['args']))
              for c in cons['eq']]))
    mieq = sum(map(len, [atleast_1d(c['fun'](x, *c['args']))
               for c in cons['ineq']]))
    # m = The total number of constraints
    m = meq + mieq
    # la = The number of constraints, or 1 if there are no constraints
    la = array([1, m]).max()
    # n = The number of independent variables
    n = len(x)

    # Define the workspaces for SLSQP
    n1 = n + 1
    mineq = m - meq + n1 + n1
    len_w = (3*n1+m)*(n1+1)+(n1-meq+1)*(mineq+2) + 2*mineq+(n1+mineq)*(n1-meq) \
            + 2*meq + n1 + ((n+1)*n)//2 + 2*m + 3*n + 3*n1 + 1
    len_jw = mineq
    w = zeros(len_w)
    jw = zeros(len_jw)

    # Decompose bounds into xl and xu
    if bounds is None or len(bounds) == 0:
        xl = np.empty(n, dtype=float)
        xu = np.empty(n, dtype=float)
        xl.fill(np.nan)
        xu.fill(np.nan)
    else:
        bnds = array([(_arr_to_scalar(l), _arr_to_scalar(u))
                      for (l, u) in bounds], float)
        if bnds.shape[0] != n:
            raise IndexError('SLSQP Error: the length of bounds is not '
                             'compatible with that of x0.')

        with np.errstate(invalid='ignore'):
            bnderr = bnds[:, 0] > bnds[:, 1]

        if bnderr.any():
            raise ValueError('SLSQP Error: lb > ub in bounds %s.' %
                             ', '.join(str(b) for b in bnderr))
        xl, xu = bnds[:, 0], bnds[:, 1]

        # Mark infinite bounds with nans; the Fortran code understands this
        infbnd = ~isfinite(bnds)
        xl[infbnd[:, 0]] = np.nan
        xu[infbnd[:, 1]] = np.nan

    # ScalarFunction provides function and gradient evaluation
    sf = _prepare_scalar_function(func, x, jac=jac, args=args, epsilon=eps,
                                  finite_diff_rel_step=finite_diff_rel_step,
                                  bounds=new_bounds)
    # gh11403 SLSQP sometimes exceeds bounds by 1 or 2 ULP, make sure this
    # doesn't get sent to the func/grad evaluator.
    wrapped_fun = _clip_x_for_func(sf.fun, new_bounds)
    wrapped_grad = _clip_x_for_func(sf.grad, new_bounds)

    # Initialize the iteration counter and the mode value
    mode = array(0, int)
    acc = array(acc, float)
    majiter = array(iter, int)
    majiter_prev = 0

    # Initialize internal SLSQP state variables
    alpha = array(0, float)
    f0 = array(0, float)
    gs = array(0, float)
    h1 = array(0, float)
    h2 = array(0, float)
    h3 = array(0, float)
    h4 = array(0, float)
    t = array(0, float)
    t0 = array(0, float)
    tol = array(0, float)
    iexact = array(0, int)
    incons = array(0, int)
    ireset = array(0, int)
    itermx = array(0, int)
    line = array(0, int)
    n1 = array(0, int)
    n2 = array(0, int)
    n3 = array(0, int)

    # Print the header if iprint >= 2
    if iprint >= 2:
        print("%5s %5s %16s %16s" % ("NIT", "FC", "OBJFUN", "GNORM"))

    # mode is zero on entry, so call objective, constraints and gradients
    # there should be no func evaluations here because it's cached from
    # ScalarFunction
    fx = wrapped_fun(x)
    try:
        fx = float(np.asarray(fx))
    except (TypeError, ValueError) as e:
        raise ValueError("Objective function must return a scalar") from e
    g = append(wrapped_grad(x), 0.0)
    c = _eval_constraint(x, cons)
    a = _eval_con_normals(x, cons, la, n, m, meq, mieq)

    while 1:
        # Call SLSQP
        slsqp(m, meq, x, xl, xu, fx, c, g, a, acc, majiter, mode, w, jw,
              alpha, f0, gs, h1, h2, h3, h4, t, t0, tol,
              iexact, incons, ireset, itermx, line,
              n1, n2, n3)

        if mode == 1:  # objective and constraint evaluation required
            fx = wrapped_fun(x)
         i-£:©([Õï*Wkögö4áÜËp*—…ï(5o«H§îªª‰ ˆégP·ÎE¤¥™õªLü0<–¼âQ¡T‚¦ÉTc\ß)O)N"z]<ìáåä ­  è
6
Ö`é¡9B£iPàÔe@Fëå¸Zm!İ·ùzÙòˆã ÎÒa ¨	òÜ˜»Şh 	Ö d<$İo4u¬Ç}CNLş.àÁ~r€r¶4ydnÕ¹	\q€»àeÑnHïUµŠq:`oñ:)(×àâXiHìuzNø´Lë±"²J·â”ïhWv$mÑáÀjG‘™b_|Æå/‡ŒAp*0²m…¹/mĞ&n~æ|ieN*ã¸ ë¸XêÍq.Ìqâc§Lq¨å«$—Õ.ql¢ïA	Wæ«#'¹ÿšîY¶hE Jÿ²
8ğ£AaCQa¡Æ‚ *¡aø°ƒŞëH`WŸ3¿PôZ:a
€0*®¼!XZ £c¢ü`z¤	`ª— 	İX{oÊ;'`ë¦¾|äÒµPZéÑ@zX=^@şùF1ØyÉş(L ¤6#0ht$”ƒ#‚ciØG%)qºèJt‰µ°6N9›" C ôi5Š0ú>c·fñ[	}l*…´`ªEt@Ê`»:švF ¿sô2åŠ‚íSú(¼q¸-33koDD¸d­N6²‚Óú &¨£è•Ü$Ò02¦J ¬A£ÄÈ®)+"p*l÷’gl¥¢“¸sœ-`nÈGæoDNºÛK^ªh
ª#„2€*!†DOFléB‰èË…#Ëi¤V/\’~¸orÂõÆ´:Í„µâUyOåB©oV`qHŠ0	æ(ë«f#f{E·Bíº	´Ï(b®
1 oêºDP ¦]â…üZ!›Š:j-ÁLMôô›B¬V)8R§~'©nùJqyawÈö’v ‘£qYÚãÍiûLtašL/qÂÔïÑÈG8b.éàoCŞîeE%Ï,£‚íÌ¬äX^4)<tÕÔfÂÿHzQğJá,á;è¶
9¡ôä’vÀ\DæEã£RhrqícvgãI ]WÌT¾G.ë¨â
ùú*ªaâCş ^ä¢("êÁÓ¬h¶$ÌU½Ï&9ŒÚ„wâ°®½pUµaêÆçë&@¤³¯'G(«‚Cïä½ıt+LšğÉE€h5:´Ü&IN2e#)ÕÄhZ‘<£
©B’E@oç2,a¨,°^* ]ü}Qry»œñÜ¥m"å[ÀT@¦(££¢u|0Ñ'¢ìºM+¼!Ë £À†€`1î÷k„JÇjcn$*XpíLb.7-X„ÌY:Œú3n¥Ïl™7i’°`-ñğ*k ì"¾åx(%¡©Êk§ƒ¾ÇÚd‡Yfp&eøx÷¿àë®„g¯2¢Ã\c-ÑéKÑfBË ¬ö<ZôJBpåíi©¶ªas/Utb®[BT)FŒ8ùR#ê²¤­=ÙjÅ¢gl±E€laæ_\;dÎgkjá‹ ğÕøê:»d¯À h[#Ö! @êo¨¸´ÀVÌØLE°¡BÂnÚ®*l©sí†o–Z}Vazy	Y8œ§Ä1«ˆè ¡Ëa#$D`9n*¤š+è4ğ(årPúd<çÍĞÉGD­H×ÇmfR¡Mjydƒ38©áPÂòÓ¹Ş.á¥¼ÅHO lÍBË`-m>,%1Lh¤k0’iâvËHä4¢uÍj«ä°¢&`ï	äEnşÛø[Á!Ä"×kJÁHüAØÓ¬‚,À¾p>ıÛIû‚^#"b½`C8ıs|…±'-*ó#L{ÂíD½5éªÕAVİ‚u§Ş#Ü`*m7ŸRîÎ®ç4ñœ &tás/Í|Ì3Âl0â"¯ ÒÜÓ ¹¶ÌuÁĞ=(U¼Al¨ °+JËŠadbbÍíŒ°/Îsõw—'HW¶ tœ%Œe*­1`¨‹Âå'â=ÏèF‚„®Äè&ÿõE<0)öRw¯ïYó)ìUÒ]®»{>`"À&ApmP$ˆ¥~/óöHMpò$)É6¶T ×sW)m)µ—ê~íàäI¨é.ğšbÉ-ïGJ,ë82+š.Í‚‘ 0Ê'úDHLˆĞ-I"7á écîĞ¨çG·yâ C3Š Q!©¥S¹ö]û
(%€´ ífD¥ûŒ1€ IÄÎÕk@X:#x©à¯ß‘1¡[l¹’(´Ám^à“áŸ¼¡«bEçTwš¨Ï.Èú];EM J*œ/îÓ¦Pãı“WC)#¼êDŒ¶Ê€o{H„gfÅn$kZÍb)/ÇÖU`/ä)åó9Rƒg‹vRáäU($cÀ{ƒá‚ãäÕÅ%ìN+üol{µDW8[Á(¼ÎCm/Ñ*&ü‘@äé¢W†3I$8m£Q‘åÏ>b,ê ­à¡«?Ù4©ƒ·`e#dŞUz…?Ó.ÂvYñ“ë'mÉ³$C‡fŞq˜fX'9ê\~gê¿ø'¬SnT6¤®¦›0PL…`L8•ÌåÃr"Ë
ˆ)áÿ0A3‹K2ƒ.Át¤Û[Ïà_Î«#¨.½#q (`‚Dfd!J¤`w;¡åItëZCÅ@ÛD­Z mûZ+os&Şõ‰Â"`°fÈføÇX`Ò"ÉFÓN¨{Gõ:Òy {ÏI+ÀCjû¼ `oec6ÎVOû_¤Øµ`m‹(,æØlÀüg@,*9d,#Ü–Q‹u­ç[KĞcÔZ¤'˜ÆîJ«Áş%eºˆ>¤òßtCsK|«ú¦~IGX5õ•(x¼ßv,#Œq~3x"¨a2´D0ÀY2¨mâ³$ÀPAKNIl2ZvÂ?inaBî@mzSß§=‘Çaï™T5@”"ÅJPÌÿ	¸mşEy"-˜uh£õÿ(™oæ–¼WØ#æô€ôê²8D.A(lyÜì1ÿ¨[Æq°ì Ñ¿(Äp,ıNfˆŸªš(œ¬~&GàGÄ_Eeæ !wv°f/3AšJ=§aØc?æJ~GoFÇ¤è÷È¢ALs‚9]ÂÃh(Õÿƒ«4Ñ£q¹6}2ïè‹UÂiuVpàğ÷–+ •¨ğÂ´ñ%Z5Yáà@¤çó,é$g8è:õÏ4@»$6©‹t…8¨wãy¼òÏ>‰+çEcMàg¿	õÉ~ …}Î(#)û¡áœ,üUTg8º¢ 4MpCZ!¿]¼,Ó×Â¹$ñcÈ2AO@É%<iÅkD¬Âz¼_öN!ë0SG¹ÓÕ¥4Mİ	ó­&*a'æ~“ &In€%ÈfÏ&;`°m4wïfûlX«*8$ø­ìÄan+µD@êr7z-å*¼¨0äGzTy˜É-ÈÀ„yÅ¦ÙcäÍmzFËfå«ö)Ú6öIoë vká ¢
êN§â\5ül7e²p³»h3•r„bM^ŠˆÍÊ°ÈnÄËªÄ©•‚6=¤Y,JHÌÔ’>‡Ú!o*r.¤ñ…À"Ò6¡h"n™(((à £0ólR8~XT8dP}1°ˆW×„êàSñÎkÒ‚ÂşÂ1[ıÛ l`ƒ>ˆ5j¦ ¡6ŸHè4ˆ ˜î‹üä“  —Iû "ÅÁìh‰sáælÕÄIë` xY}EyPaq_jBxü9G©¾µšz¯Â_ªi¼í6×'2@ŠbB*Æ›^ nÑ"|ø™õ=¸Å@ó/bÀ"uñ³f'Iı·J2oôx

’pÀmnAD5¬>Œ\@OexË
`"*0Î"¬,áE °Nd¹ì÷j\q‚ë·pØÖ©üæ×´Q00Ğs²èœ6ˆìùĞ÷^Ì¤É1‹;’ĞÂÀİiÚ¨#,UxİX6¬
x’åŠ‚":"x	0œmí™õm›QÅYµ#Ä Ë²'èb¼ 8ÛxMsKËÒ:ÿd ufB.!lÙZoFN›dõeñ-³.KªÂŠ,¸¨3Lm”añtö±S`Üé8q¢Z²Ù­`}èM¨á;/4ƒuÇ	RG—tQkß<YøŞÀp¤cSò„)c…ò¸Hª¨õìq+=é(àp ÑşÁÚpÖ;Irşƒ$R*S("Pî!†T©€›áZ¨0@Å’A){ÕãTí§‘¨ B*ë}¢ƒè.,ì‰Aq !lgV)câÑÀcŒÄX,;#3‡6È2êå¡ E8ÖQssBï€4ìú*#-æ*»ÙÚÉë{v‘p¡nÈ,Va¸	‹Ã¡KÂ ¤Í¦6f}¼ce†ğ9*EÎÅ™2/MLO‚º& ‰–Y£´S-~û®kdÀá|wë}£ïñ"û ÎûknY'( ¤&»û@¯%ÍƒGIµF_‚:¸ºÒ7R(o9;neï€Fv*Pl¿ ÂÇ§¤8QÓíg m1Â¯Âyú ˆ$º"F ¢,ãfÚ¤\{%ë+ŸÄk,‹)`úw…Ï‡Ë uMYIí88Êgˆ—š(hçSc¿Ú}¡4`P+	z)ˆ³©eÜY‰“	i,®—.w¤m8íeq`B“ ÓÿÌgUü° XÍB2)I"¥²³9u„nD€ÀÖÉİ.üs`†£jHdzDlfîxø®¦şgî¾@R!A"[±ñy«°ìÉ¤1ÛO@O®m &	„ë™Û?{•¨g—øÊøÖ€S ¿dtY}4xdÀS¾üÂ›¥`ãB
H1:,-CĞ3Z=Œ€ ÏVæ`¿ÂÅ‡	 WdMÏ·aµ!*ı;g h0¯lë	*	• 1®‹æ@\6•”ùR|[x|ï#µ1²1‡¢Îæ‘zà{J>ÌqhS}74aınyğ`T-.n¥Râş[m2¨ûm10û
rgÌ’¯j;Y%¤7ø—aAr“ 3 X)QàƒS~¹´»sW,|OPÕda É"Ph*»£P@íP˜!¾‘Å¶, %š&šjˆ;A IÅËã²%ìCBÁ~¨Ê1ñ¸vo{Éô‘·ù’r8diCeù³ï{Zõ!}Ns²p÷fª¯ªvdRî¤µ¤S;g pï
Íı@%0/d;l8Ò@MÕ'&ql%1GíÌd 0o¤&Ótè¦&İ·\§°ça¸Êıv$±~ñ½f¼	Tø’Ù—xV„eäs:ªÈÿK04rhıR	ïº€á˜½ŞY=‹Ì7TİÕç|á}L\ÌW\, 'vø‡oeù ‹Û£>,€„,xd ”ÒJ  CŠa Û"‹po:g€eécÉ/¢ÄamÊ%àâÉq}a17 ¼[^¬}±ñØPî¬òàkHĞ!88
€°{t°@‚ft
Ê5®Vm 7ëm[«RÓEVDO.,ß ¼´_Mc=«ÁXdÅÆmël©m‰íF„ j"í¬hå(yB²v"x6¼L¨sJ¶– ğçè’XÖypıæùto5E±+î¦EÄ9ş§2E[x $rnáé_{åíÈŠ}×2œnB6 M
¥¨,²K]Éı+ï¨S ùíéÚ/—óK|º/37Æ-÷ ¹¯R–o‰8D`4 7ÃŒ?¯àlÕ_µ?0oÌß® ^e®#’! §¯ ñ=IŞŒ"`c8eA_©g­¬Š3#€Ôóg,u€5